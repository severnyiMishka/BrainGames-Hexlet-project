name: run linter

on: push

jobs:
  my-job:

    runs-on: ubuntu-latest

    steps:

    uses: actions

    - name: npm install
      run: |
        make install
      env: 
        CI: true

    - name: run linter
      run: |
        make lint
