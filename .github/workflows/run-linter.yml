name: run linter

on: push

jobs:
  my-job:

    runs-on: ubuntu-latest

    steps:
    
    - name: npm install
      run: |
        make install

    - name: run linter
      run: |
        make lint
